{
  "properties": [
    {
      "name": "csproj_formatter_sort_entries",
      "description": "Sorts supported PropertyGroup and ItemGroup entries alphabetically while preserving dependency order.",
      "values": [true, false],
      "defaultValue": [true],
      "severity": false,
      "documentationLink": "https://github.com/stefanegli/CsProjFormatter#editorconfig",
      "example": "csproj_formatter_sort_entries = true"
    },
    {
      "name": "csproj_formatter_empty_lines_between_groups",
      "description": "Controls how many empty lines are inserted between top-level groups. Use a non-negative integer; 0 disables extra blank lines.",
      "defaultValue": [1],
      "severity": false,
      "documentationLink": "https://github.com/stefanegli/CsProjFormatter#editorconfig",
      "example": "csproj_formatter_empty_lines_between_groups = 1"
    },
    {
      "name": "csproj_formatter_sort_item_types",
      "description": "Case-insensitive replacement for the built-in item-type list. Listed types are eligible for safe sorting and attribute/metadata canonicalization; unlisted types retain those orders. Use * to allow every item type.",
      "defaultValue": ["AdditionalFiles, Analyzer, ApplicationDefinition, AssemblyAttribute, AssemblyMetadata, Compile, CompilerVisibleItemMetadata, CompilerVisibleProperty, COMFileReference, COMReference, Content, EditorConfigFiles, EmbeddedResource, Folder, FrameworkReference, GlobalAnalyzerConfigFiles, InternalsVisibleTo, NativeReference, None, PackageDownload, PackageReference, Page, ProjectReference, PrunePackageReference, Reference, Resource, RuntimeHostConfigurationOption, SplashScreen, TrimmerRootAssembly, Using"],
      "severity": false,
      "documentationLink": "https://github.com/stefanegli/CsProjFormatter#editorconfig",
      "example": "csproj_formatter_sort_item_types = PackageReference, Protobuf"
    }
  ]
}
