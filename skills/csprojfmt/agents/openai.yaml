interface:
  display_name: "CsProjFormatter CLI"
  short_description: "Configure, lint, and format SDK-style projects"
  default_prompt: "Use $csprojfmt to configure, lint, and format the .csproj files in this repository."
